{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Vite + Chrome Debug",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8080/presets.html",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "run: all"
    }
  ]
}
